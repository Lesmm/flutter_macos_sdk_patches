diff --git a/bin/internal/shared.sh b/bin/internal/shared.sh
index d78e0cd11fe..963a118885d 100644
--- a/bin/internal/shared.sh
+++ b/bin/internal/shared.sh
@@ -204,6 +204,7 @@ function shared::execute() {
 
   FLUTTER_TOOLS_DIR="$FLUTTER_ROOT/packages/flutter_tools"
   SNAPSHOT_PATH="$FLUTTER_ROOT/bin/cache/flutter_tools.snapshot"
+  # SNAPSHOT_PATH="$FLUTTER_TOOLS_DIR/bin/flutter_tools.dart"       # same as $SCRIPT_PATH
   STAMP_PATH="$FLUTTER_ROOT/bin/cache/flutter_tools.stamp"
   SCRIPT_PATH="$FLUTTER_TOOLS_DIR/bin/flutter_tools.dart"
   DART_SDK_PATH="$FLUTTER_ROOT/bin/cache/dart-sdk"
@@ -270,6 +271,11 @@ function shared::execute() {
     flutter*)
       # FLUTTER_TOOL_ARGS aren't quoted below, because it is meant to be
       # considered as separate space-separated args.
+      echo ""
+      echo "📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎"
+      echo ""$DART" --packages="$FLUTTER_TOOLS_DIR/.dart_tool/package_config.json" $FLUTTER_TOOL_ARGS "$SNAPSHOT_PATH" "$@""
+      echo "📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎📎"
+      echo ""
       exec "$DART" --packages="$FLUTTER_TOOLS_DIR/.dart_tool/package_config.json" $FLUTTER_TOOL_ARGS "$SNAPSHOT_PATH" "$@"
       ;;
     dart*)
diff --git a/packages/flutter_tools/bin/podhelper.rb b/packages/flutter_tools/bin/podhelper.rb
index 8ee921bfebb..ce6c72efe55 100644
--- a/packages/flutter_tools/bin/podhelper.rb
+++ b/packages/flutter_tools/bin/podhelper.rb
@@ -189,12 +189,14 @@ end
 # @param [String] ios_application_path Path of the iOS directory of the Flutter app.
 #                                      Optional, defaults to the Podfile directory.
 def flutter_install_all_ios_pods(ios_application_path = nil)
+  puts "[IOS] Installing Flutter iOS pods: #{ios_application_path}"
   flutter_install_ios_engine_pod(ios_application_path)
   flutter_install_plugin_pods(ios_application_path, '.symlinks', 'ios')
 end
 
 # Same as flutter_install_all_ios_pods for macOS.
 def flutter_install_all_macos_pods(macos_application_path = nil)
+  puts "[IOS] Installing Flutter macos pods: #{macos_application_path}"
   flutter_install_macos_engine_pod(macos_application_path)
   flutter_install_plugin_pods(macos_application_path, File.join('Flutter', 'ephemeral', '.symlinks'), 'macos')
 end
@@ -242,6 +244,7 @@ def flutter_install_ios_engine_pod(ios_application_path = nil)
   end
 
   # Keep pod path relative so it can be checked into Podfile.lock.
+  puts "🍏 pod 'Flutter', path: #{flutter_relative_path_from_podfile(podspec_directory)}"
   pod 'Flutter', path: flutter_relative_path_from_podfile(podspec_directory)
 end
 
@@ -302,6 +305,7 @@ def flutter_install_plugin_pods(application_path = nil, relative_symlink_dir, pl
   system('mkdir', '-p', symlink_plugins_dir)
 
   plugins_file = File.join(application_path, '..', '.flutter-plugins-dependencies')
+  puts "[IOS] Reading flutter plugins: #{plugins_file}"
   dependencies_hash = flutter_parse_plugins_file(plugins_file)
   plugin_pods = flutter_get_plugins_list(dependencies_hash, platform)
   swift_package_manager_enabled = flutter_get_swift_package_manager_enabled(dependencies_hash, platform)
@@ -318,6 +322,7 @@ def flutter_install_plugin_pods(application_path = nil, relative_symlink_dir, pl
     next unless plugin_name && plugin_path && has_native_build
     symlink = File.join(symlink_plugins_dir, plugin_name)
     File.symlink(plugin_path, symlink)
+    puts "[IOS] Symlink native build plugin: #{symlink} -> #{plugin_path}"
 
     # Keep pod path relative so it can be checked into Podfile.lock.
     relative = flutter_relative_path_from_podfile(symlink)
@@ -331,6 +336,7 @@ def flutter_install_plugin_pods(application_path = nil, relative_symlink_dir, pl
     # The tool will print an error about it.
     next if swift_package_exists && !File.exist?(File.join(relative, platform_directory, plugin_name + ".podspec"))
 
+    puts "🍏 pod '#{plugin_name}', path: #{File.join(relative, platform_directory)}"
     pod plugin_name, path: File.join(relative, platform_directory)
   end
 end
diff --git a/packages/flutter_tools/lib/executable.dart b/packages/flutter_tools/lib/executable.dart
index 56ead85cb02..2acc822912c 100644
--- a/packages/flutter_tools/lib/executable.dart
+++ b/packages/flutter_tools/lib/executable.dart
@@ -98,6 +98,8 @@ Future<void> main(List<String> args) async {
     userMessages: UserMessages(),
   );
 
+  print('💯 ##### [executable.dart] main with args: $args \n');
+
   await runner.run(
     args,
     () => generateCommands(verboseHelp: verboseHelp, verbose: verbose),
@@ -152,8 +154,8 @@ Future<void> main(List<String> args) async {
   );
 }
 
-List<FlutterCommand> generateCommands({required bool verboseHelp, required bool verbose}) =>
-    <FlutterCommand>[
+List<FlutterCommand> generateCommands({required bool verboseHelp, required bool verbose}) {
+  List<FlutterCommand> commands =  <FlutterCommand>[
       AnalyzeCommand(
         verboseHelp: verboseHelp,
         fileSystem: globals.fs,
@@ -264,6 +266,9 @@ List<FlutterCommand> generateCommands({required bool verboseHelp, required bool
       IdeConfigCommand(),
       UpdatePackagesCommand(verboseHelp: verboseHelp),
     ];
+  print('💯 ##### [executable.dart] generate flutter commands: $commands \n');
+  return commands;
+}
 
 /// An abstraction for instantiation of the correct logger type.
 ///
diff --git a/packages/flutter_tools/lib/runner.dart b/packages/flutter_tools/lib/runner.dart
index 94b17cd003d..394bf647f9f 100644
--- a/packages/flutter_tools/lib/runner.dart
+++ b/packages/flutter_tools/lib/runner.dart
@@ -46,6 +46,8 @@ Future<int> run(
   }
 
   return runInContext<int>(() async {
+    print('💯 ##### [runner.dart] running ... \n');
+
     globals.terminal.applyFeatureFlags(featureFlags);
 
     reportCrashes ??= !await globals.isRunningOnBot;
diff --git a/packages/flutter_tools/lib/src/android/android_studio.dart b/packages/flutter_tools/lib/src/android/android_studio.dart
index 0c773437659..1e45f57db1c 100644
--- a/packages/flutter_tools/lib/src/android/android_studio.dart
+++ b/packages/flutter_tools/lib/src/android/android_studio.dart
@@ -40,6 +40,7 @@ class AndroidStudio {
     this.presetPluginsPath,
   }) {
     _initAndValidate();
+    globals.printStatus('[ANDROID_STUDIO] initialized: $this');
   }
 
   static AndroidStudio? fromMacOSBundle(String bundlePath, {String? configuredPath}) {
@@ -227,6 +228,7 @@ class AndroidStudio {
 
     // Find all available Studio installations.
     final List<AndroidStudio> studios = allInstalled();
+    globals.printStatus('[ANDROID_STUDIO] Fetch All installed studios: $studios');
     if (studios.isEmpty) {
       return null;
     }
@@ -241,9 +243,21 @@ class AndroidStudio {
         .firstOrNull;
 
     if (manuallyConfigured != null) {
+      globals.printStatus('[ANDROID_STUDIO] Using configured AS: $manuallyConfigured');
       return manuallyConfigured;
     }
 
+    String? runningStudioPath = getTheRunningStudioAppPath();
+    if (runningStudioPath != null) {
+      for (final AndroidStudio studio in studios) {
+        final dir = studio.directory;
+        if (dir.contains(runningStudioPath) || runningStudioPath.contains(dir)) {
+          globals.printStatus('[ANDROID_STUDIO] Using the running studio: $studio');
+          return studio;
+        }
+      }
+    }
+
     AndroidStudio? newest;
     for (final AndroidStudio studio in studios.where((AndroidStudio s) => s.isValid)) {
       if (newest == null) {
@@ -265,9 +279,42 @@ class AndroidStudio {
       }
     }
 
+    globals.printStatus('[ANDROID_STUDIO] Using the newest studio: $newest');
     return newest;
   }
 
+  static String? getTheRunningStudioAppPath() {
+    try {
+      // PID & Parent PID
+      globals.printStatus('[ANDROID_STUDIO] My process pid: $pid');
+      String? ppid;
+      RunResult? result;
+      result = globals.processUtils.runSync(<String>['ps', '-o', 'ppid=', '-p', '$pid']);
+      if (result != null && result.exitCode == 0) {
+        ppid = result.stdout.trim();
+      }
+      globals.printStatus('[ANDROID_STUDIO] My process parent pid: $ppid');
+      // Get parent process name
+      if (ppid != null) {
+        String? parentProcessName;
+        result = globals.processUtils.runSync(<String>['ps', '-o', 'comm=', '-p', ppid]);
+        if (result != null && result.exitCode == 0) {
+          parentProcessName = result.stdout.trim();
+        }
+        globals.printStatus('[ANDROID_STUDIO] My process parent process name: $parentProcessName');
+        // Substring the .app path from the parent process name
+        int? end = parentProcessName?.indexOf('.app');
+        String? runningStudioAppPath = end != null && end > 0 ? parentProcessName?.substring(0, end + 4) : null;
+        globals.printStatus('[ANDROID_STUDIO] My process parent process app: $runningStudioAppPath');
+        return runningStudioAppPath;
+      }
+    } on ProcessException catch (e, s) {
+      // nothing now ...
+      globals.printStatus('[ANDROID_STUDIO] Error: $e\n$s');
+    }
+    return null;
+  }
+
   static List<AndroidStudio> allInstalled() =>
       globals.platform.isMacOS ? _allMacOS() : _allLinuxOrWindows();
 
@@ -596,7 +643,7 @@ the configured path by running this command: flutter config --android-studio-dir
   }
 
   @override
-  String toString() => 'Android Studio ($version)';
+  String toString() => 'Android Studio ($version), path: $directory, java path: $javaPath';
 }
 
 bool _pathsAreEqual(String path, String other) {
diff --git a/packages/flutter_tools/lib/src/android/gradle.dart b/packages/flutter_tools/lib/src/android/gradle.dart
index ac3b3f7831c..048e5ad09a9 100644
--- a/packages/flutter_tools/lib/src/android/gradle.dart
+++ b/packages/flutter_tools/lib/src/android/gradle.dart
@@ -349,6 +349,12 @@ class AndroidGradleBuilder implements AndroidBuilder {
     ];
     preRunTask?.call();
 
+    _logger.printStatus('Building android app... \n'
+        '🥎🥎🥎 command: $command, \n'
+        '🥎🥎🥎 working directory: ${project.android.hostAppGradleRoot.path}, \n'
+        '🥎🥎🥎 environment: ${_java?.environment} \n'
+    );
+
     var exitCode = 1;
     try {
       exitCode = await _processUtils.stream(
@@ -573,6 +579,11 @@ class AndroidGradleBuilder implements AndroidBuilder {
     if (androidBuildInfo.fastStart) {
       options.add('-Pfast-start=true');
     }
+
+    _logger.printStatus('Building android with gradle path: $gradleExecutablePath');
+    _logger.printStatus('Building android with gradle task: $assembleTask');
+    _logger.printStatus('Building android with gradle options: $options');
+
     late Stopwatch sw;
     final int exitCode = await _runGradleTask(
       assembleTask,
diff --git a/packages/flutter_tools/lib/src/base/config.dart b/packages/flutter_tools/lib/src/base/config.dart
index 47f58b63058..f1b31a39853 100644
--- a/packages/flutter_tools/lib/src/base/config.dart
+++ b/packages/flutter_tools/lib/src/base/config.dart
@@ -180,6 +180,7 @@ class Config {
     String json = const JsonEncoder.withIndent('  ').convert(_values);
     json = '$json\n';
     _file.writeAsStringSync(json);
+    _logger.printStatus('[DEBUG] Wrote Flutter config to ${_file.path}');
   }
 
   // Reads the process environment to find the current user's home directory.
diff --git a/packages/flutter_tools/lib/src/base/logger.dart b/packages/flutter_tools/lib/src/base/logger.dart
index e19a4dbe5ba..a73c3f78c0f 100644
--- a/packages/flutter_tools/lib/src/base/logger.dart
+++ b/packages/flutter_tools/lib/src/base/logger.dart
@@ -558,7 +558,8 @@ class StdoutLogger extends Logger {
   void writeToStdErr(String message) => _stdio.stderrWrite(message);
 
   @override
-  void printTrace(String message) {}
+  // void printTrace(String message) {}
+  void printTrace(String message) => message.length > 2048 ? _stdio.stdoutWrite('✅### TL>>>size: ${message.length}, ${message.substring(0, 512)}...\n') : _stdio.stdoutWrite('✅### $message\n');
 
   @override
   Status startProgress(
diff --git a/packages/flutter_tools/lib/src/base/process.dart b/packages/flutter_tools/lib/src/base/process.dart
index c46c3e6803e..9f99ac1d761 100644
--- a/packages/flutter_tools/lib/src/base/process.dart
+++ b/packages/flutter_tools/lib/src/base/process.dart
@@ -326,7 +326,7 @@ class _DefaultProcessUtils implements ProcessUtils {
     if (timeoutRetries < 0) {
       throw ArgumentError('timeoutRetries must be non-negative');
     }
-    _traceCommand(cmd, workingDirectory: workingDirectory);
+    _traceCommand(cmd, workingDirectory: workingDirectory, environment: environment);
 
     // When there is no timeout, there's no need to kill a running process, so
     // we can just use _processManager.run().
@@ -445,7 +445,7 @@ class _DefaultProcessUtils implements ProcessUtils {
     bool allowReentrantFlutter = false,
     Encoding encoding = systemEncoding,
   }) {
-    _traceCommand(cmd, workingDirectory: workingDirectory);
+    _traceCommand(cmd, workingDirectory: workingDirectory, environment: environment);
     final ProcessResult results = _processManager.runSync(
       cmd,
       workingDirectory: workingDirectory,
@@ -499,7 +499,7 @@ class _DefaultProcessUtils implements ProcessUtils {
     Map<String, String>? environment,
     ProcessStartMode mode = ProcessStartMode.normal,
   }) {
-    _traceCommand(cmd, workingDirectory: workingDirectory);
+    _traceCommand(cmd, workingDirectory: workingDirectory, environment: environment);
     return _processManager.start(
       cmd,
       workingDirectory: workingDirectory,
@@ -580,7 +580,7 @@ class _DefaultProcessUtils implements ProcessUtils {
 
   @override
   bool exitsHappySync(List<String> cli, {Map<String, String>? environment}) {
-    _traceCommand(cli);
+    _traceCommand(cli, environment: environment);
     if (!_processManager.canRun(cli.first)) {
       _logger.printTrace('$cli either does not exist or is not executable.');
       return false;
@@ -596,7 +596,7 @@ class _DefaultProcessUtils implements ProcessUtils {
 
   @override
   Future<bool> exitsHappy(List<String> cli, {Map<String, String>? environment}) async {
-    _traceCommand(cli);
+    _traceCommand(cli, environment: environment);
     if (!_processManager.canRun(cli.first)) {
       _logger.printTrace('$cli either does not exist or is not executable.');
       return false;
@@ -625,12 +625,16 @@ class _DefaultProcessUtils implements ProcessUtils {
     return environment;
   }
 
-  void _traceCommand(List<String> args, {String? workingDirectory}) {
+  void _traceCommand(List<String> args, {String? workingDirectory, Map<String, String>? environment }) {
     final String argsText = args.join(' ');
+    final DateTime n = DateTime.now();
+    String pad(int i) => i.toString().padLeft(2, '0');
+    final String s = '${n.year}-${pad(n.month)}-${pad(n.day)} ${pad(n.hour)}:${pad(n.minute)}:${pad(n.second)}';
+    _logger.printStatus('*$s: ${workingDirectory != null ? '[$workingDirectory/] ' : ''}$argsText');
     if (workingDirectory == null) {
-      _logger.printTrace('executing: $argsText');
+      _logger.printTrace('executing: $argsText \n with environment: $environment');
     } else {
-      _logger.printTrace('executing: [$workingDirectory/] $argsText');
+      _logger.printTrace('executing: [$workingDirectory/] $argsText \n with environment: $environment');
     }
   }
 }
diff --git a/packages/flutter_tools/lib/src/context_runner.dart b/packages/flutter_tools/lib/src/context_runner.dart
index 76d01dd6b93..b2e0dbc4716 100644
--- a/packages/flutter_tools/lib/src/context_runner.dart
+++ b/packages/flutter_tools/lib/src/context_runner.dart
@@ -77,6 +77,8 @@ Future<T> runInContext<T>(FutureOr<T> Function() runner, {Map<Type, Generator>?
     return runner();
   }
 
+  print('💯 ##### [context_runner.dart] generate globals properties ... \n');
+
   // TODO(ianh): We should split this into two, one for tests (which should be
   // in test/), and one for production (which should be in executable.dart).
   return context.run<T>(
diff --git a/packages/flutter_tools/lib/src/dart/package_map.dart b/packages/flutter_tools/lib/src/dart/package_map.dart
index b71f97c9acb..469f3a35ef4 100644
--- a/packages/flutter_tools/lib/src/dart/package_map.dart
+++ b/packages/flutter_tools/lib/src/dart/package_map.dart
@@ -60,6 +60,7 @@ Future<PackageConfig> loadPackageConfigWithLogging(
   required Logger logger,
   bool throwOnError = true,
 }) async {
+  logger.printStatus('💯 ##### [package_map.dart] Loading package config from: $file ...');
   final FileSystem fileSystem = file.fileSystem;
   var didError = false;
   final PackageConfig result = await loadPackageConfigUri(
@@ -93,5 +94,6 @@ Future<PackageConfig> loadPackageConfigWithLogging(
   if (didError) {
     throwToolExit('');
   }
+  logger.printStatus('💯 ##### [package_map.dart] Loading package config result: $result ...');
   return result;
 }
diff --git a/packages/flutter_tools/lib/src/flutter_plugins.dart b/packages/flutter_tools/lib/src/flutter_plugins.dart
index 5778c6140c2..3696ec3247a 100644
--- a/packages/flutter_tools/lib/src/flutter_plugins.dart
+++ b/packages/flutter_tools/lib/src/flutter_plugins.dart
@@ -68,6 +68,7 @@ Future<Plugin?> _pluginFromPackage(
   }
   Object? pubspec;
 
+  // globals.printStatus('💯 ##### [flutter_plugins.dart] Reading file: ${pubspecFile.path}');
   try {
     pubspec = loadYaml(await pubspecFile.readAsString());
   } on YamlException catch (err) {
@@ -208,6 +209,7 @@ bool _writeFlutterPluginsList(
   required bool swiftPackageManagerEnabledMacos,
 }) {
   final File pluginsFile = project.flutterPluginsDependenciesFile;
+  globals.logger.printStatus('💯 ##### [flutter_plugins.dart] Writing content to .flutter-plugins-dependencies: $pluginsFile ...');
   if (plugins.isEmpty) {
     return ErrorHandlingFileSystem.deleteIfExists(pluginsFile);
   }
diff --git a/packages/flutter_tools/lib/src/runner/flutter_command.dart b/packages/flutter_tools/lib/src/runner/flutter_command.dart
index 38853cca68e..91663d47653 100644
--- a/packages/flutter_tools/lib/src/runner/flutter_command.dart
+++ b/packages/flutter_tools/lib/src/runner/flutter_command.dart
@@ -2,6 +2,8 @@
 // Use of this source code is governed by a BSD-style license that can be
 // found in the LICENSE file.
 
+import 'dart:io' as dart_io;
+
 import 'package:args/args.dart';
 import 'package:args/command_runner.dart';
 import 'package:file/file.dart';
@@ -2002,6 +2004,28 @@ abstract class FlutterCommand extends Command<void> {
           'Changing current working directory to: ${globals.fs.currentDirectory.path}',
         );
       }
+
+      /// If debugging 'flutter_tools' source codes
+      const String flutter_tools = 'packages/flutter_tools';
+      if (globals.fs.currentDirectory.path.contains(flutter_tools)) {
+        globals.printStatus('[DEBUG] Detected flutter_tools source code debugging...');
+        final String? projectRoot = stringArg('project-root');
+        if (projectRoot != null) {
+          // 1. change to project root
+          globals.fs.currentDirectory = projectRoot;
+        } else {
+          // 2. change to the target file's parent directory that with pubspec.yaml located in
+          final String targetPath = targetFile;
+          if (targetPath.startsWith('/')) {
+            final String? path = findProjectRoot(globals.fs, dart_io.File(targetPath).parent.path);
+            if (path != null) {
+              globals.fs.currentDirectory = path;
+            }
+          }
+        }
+        globals.printStatus('[DEBUG] Debugging flutter_tools in path: ${globals.fs.currentDirectory}');
+      }
+
     }
 
     if (_usesTargetOption) {
